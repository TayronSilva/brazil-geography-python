# Brazilian Geography CLI 🇧🇷 🐍

A Python-based Command Line Interface (CLI) tool designed to manage and query Brazilian states and cities data from CSV files. 

## ✨ Features
- **State Listing:** Lists all Brazilian states without duplicates.
- **Smart Search:** Find which state a city belongs to using an optimized dictionary lookup.
- **Data Export:** Generate CSV reports from the processed data.
- **Interactive Menu:** User-friendly terminal interface.

## 🛠️ Technologies
- **Python 3.x**
- **CSV Module** (Native data handling)

## 🚀 How to run
1. Clone the repository or download the files.
2. Ensure you have `states.csv` and `cities.csv` in the same folder.
3. Run the script:
   ```bash
   python main.py
